#!/usr/bin/env bash
# OBSOLETE as a deploy tool. Owner policy 2026-08-23: deploys are local via
# bin/deploy-local; GH Actions is dispatch-only, on explicit request.
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "bin/deploy is obsolete. Use: $DIR/deploy-local (local build + gh-pages push)."
exit 1
